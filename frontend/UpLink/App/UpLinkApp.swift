// UpLinkApp.swift

// the starting point of the app; calls RootView to handle routing

import SwiftUI

@main
struct UpLinkApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
